モジュール
______________________________________
    それ単体で完結しているもの(class, 関数群).

    このシステムから切り離されても動作する.

    phpとして実行しても, 何もしないこと. 

    モジュールがモジュール同士で機能を補うのはよろしい.
    その時, includeするときはこのフォルダ内で完結する.

    ただ, 一部システムの設定を参照する必要があるものは(ContentsDatabaseなど)は,
    このフォルダの上位にある設定スクリプト(ContentsManagementSystem.php)を参照してもよい.